const styles = {
  mainWindow: {
    overflowY: 'scroll'
  },
  cardsContainer: {
    display: 'grid',
    gridTemplate: 'repeat(4, 262px) / repeat(4, 182px)',
    gridGap: '20px 30px',
    justifyContent: 'center',
    alignContent: 'center',
    padding: '40px'
  }
};

export default styles;
